# Introduction
A test application, used to check that the libraries which connect with the front garden railway server do their job and that the protocol works.

It is best to open this project in Visual Studio Code using the `Open Workspace from File` option and selecting [`test.code-workspace`](test.code-workspace): that way it will open not only the current project but also the components that the project may depend on.
